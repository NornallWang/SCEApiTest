package com.company.lorna.main;/** * Created by goyoo on 2017/6/2. * 检查str2字符串内容是否在str1中出现； * 如果出现，出现了几次 * 分别在什么位置出现的 */public class StrContains {    public static void main(String args[]){        StrContains strContains = new StrContains();        strContains.check("asdfsfsedfsegegefsdsdscfs","fs");    }    public void check(String str1,String str2){        int sum = 0;        int str1length = str1.length();        int str2length = str2.length();        //如果str1的字符串长度比str2的还要小，则说明str1中肯定不存在str2整个字符串        if (str1length < str2length){            System.out.println("str2字符串在str1中不存在");        }        //从str1的第一个位置的字符开始进行比对        for (int i=0;i<str1length;i++) {            //加入str1的长度减去依旧大于等于str2的长度，则继续查找            if(str1length-i>=str2length) {                //定义一个临时变量，将在str1中开始查找到字符串保存起来                String tempStr = str1.substring(i, str2length + i);                if (tempStr.equals(str2)) {                    sum = sum + 1;                    System.out.println("在str1字符串的第 " + i + " 个位置出现了str2的字符串");                }            }        }        //如果str1的长度键入i小于str2的长度，则结束循环        System.out.println("str2的字符串在str1中总共出现了 "+sum+" 次");    }}